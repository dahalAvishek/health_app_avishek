import 'package:flutter/widgets.dart';

class AccessmentCard extends StatelessWidget {
  const AccessmentCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
